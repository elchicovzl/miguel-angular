'use strict';

// (function() {
    var lodash;

    lodash  = require('lodash');

    module.exports = [function() {
        var helpers;

        helpers = {};

        lodash.extend(helpers, lodash);

        return helpers;
    }]
// })();